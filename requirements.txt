# GoChange — dépendances
Django==6.0.1
python-dotenv==1.1.0
requests==2.32.5
Pillow==12.2.0
whitenoise==6.12.0
django-jazzmin==3.0.4

# PostgreSQL (prod) — déjà présent ; SQLite suffit en dev
psycopg[binary]==3.3.3

# Serveur de production
gunicorn==22.0.0
